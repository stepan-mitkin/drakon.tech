function ThemeUtils(undefined) {
	var self = this;

var module = this
module.props = {}
module.icons = {}
var blue = "#A2B7F4"

// Autogenerated with DRAKON Editor 1.33


function buildBlue() {
    var props = {}
    var icons = {}
    props.back="white"
    props.line="black"
    props.iconline="none"
    props.backtext="black"
    props.iconback="#003789"
    props.iconback2="#2A529A"
    props.shadow="100"
    props.thickness=1
    props.style="solid"
    props.operator = "#ffffff"
    props.keyword = "#70A5FF"
    props.number = "#ffffff"
    props.string = "#d0ffd0"
    props.comment = "#808080"
    props.identifier = "#BCD5FF"
    props.error = "#ff0000"
    icons.question = {
    	iconback: "#305FB5"
    }
    icons.select = {
    	iconback: "#305FB5"
    }
    icons["case"] = icons.select
    icons.loopbegin = {
    	iconback: "#3E3B3B"
    }
    icons.loopend = icons.loopbegin
    return {
        props : props,
        icons : icons
    }
}

function buildCloud() {
    var props = {}
    var icons = {}
    props.back="#DADEFF"
    props.iconline = "#606060"
    props.iconback="#ffffff"
    props.iconback2="#ffffff"
    props.shadow="none"
    props.thickness=1
    props.style="solid"
    props.operator = "#cc3399"
    props.keyword = "#003399"
    props.number = "#004d00"
    props.string = "#800000"
    props.comment = "#995c00"
    props.error = "#ff0000"
    return {
        props : props,
        icons : icons
    }
}

function buildDark1() {
    var props = {}
    var icons = {}
    props.back="#202020"
    props.line="#dddd90"
    props.iconline="#98984D"
    props.backtext="#ffffcc"
    props.iconback="#000000"
    props.iconback2="#303030"
    props.shadow="none"
    props.thickness=1
    props.style="solid"
    props.operator = "#95DCFC"
    props.keyword = "#80ff80"
    props.number = "#ffffff"
    props.string = "#CF8E60"
    props.comment = "#808080"
    props.identifier = "#ffffcc"
    props.error = "#ff0000"
    return {
        props : props,
        icons : icons
    }
}

function buildDark2() {
    var props = {}
    var icons = {}
    props.back="#202020"
    props.line="#dddd90"
    props.iconline="#98984D"
    props.backtext="#ffffcc"
    props.iconback="#000000"
    props.iconback2="#303030"
    props.shadow="none"
    props.thickness=1
    props.style="solid"
    props.operator = "#ff00ff"
    props.keyword = "#00ff00"
    props.number = "#ffffff"
    props.string = "#ff6600"
    props.comment = "#808080"
    props.identifier = "#ffffcc"
    props.error = "#ff0000"
    return {
        props : props,
        icons : icons
    }
}

function buildDefaultTheme() {
    var props = {}
    var icons = {}
    props.back=blue
    props.iconline = "none"
    props.iconback="#ffffff"
    props.iconback2="#ffffff"
    props.shadow="100"
    props.thickness=1
    props.style="solid"
    props.operator = "#cc3399"
    props.keyword = "#003399"
    props.number = "#004d00"
    props.string = "#800000"
    props.comment = "#995c00"
    props.error = "#ff0000"
    return {
        props : props,
        icons : icons
    }
}

function buildNight() {
    var props = {}
    var icons = {}
    props.back="#000040"
    props.line="#7070c0"
    props.iconline="#5050b0"
    props.backtext="#BCD5FF"
    props.iconback="#000000"
    props.iconback2="#000030"
    props.shadow="200"
    props.thickness=1
    props.style="solid"
    props.operator = "#ffffff"
    props.keyword = "#70A5FF"
    props.number = "#ffffff"
    props.string = "#6882aF"
    props.comment = "#808080"
    props.identifier = "#BCD5FF"
    props.error = "#ff0000"
    return {
        props : props,
        icons : icons
    }
}

function buildParrot() {
    var props = {}
    var icons = {}
    props.back=blue
    props.iconline = "none"
    props.iconback="#ffffff"
    props.iconback2="#ffffff"
    props.shadow="100"
    props.thickness=1
    props.style="solid"
    props.operator = "#cc3399"
    props.keyword = "#003399"
    props.number = "#004d00"
    props.string = "#800000"
    props.comment = "#995c00"
    props.error = "#ff0000"
    icons.question = {
    	iconback: "#ffffcc"
    }
    icons.select = {
    	iconback: "#ffecec"
    }
    icons["case"] = icons.select
    icons.loopbegin = {
    	iconback: "#ccffcc"
    }
    icons.loopend = icons.loopbegin
    return {
        props : props,
        icons : icons
    }
}

function buildRedBlue() {
    var props = {}
    var icons = {}
    props.back="white"
    props.line="black"
    props.iconline="none"
    props.backtext="black"
    props.iconback="#003789"
    props.iconback2="#2A529A"
    props.shadow="100"
    props.thickness=1
    props.style="solid"
    props.operator = "#ffffff"
    props.keyword = "#ffffd0"
    props.number = "#ffffff"
    props.string = "#d0ffd0"
    props.comment = "#808080"
    props.identifier = "#ffffd0"
    props.error = "#ff0000"
    icons.question = {
    	iconback: "#7E1A1D"
    }
    icons.select = {
    	iconback: "#7E1A1D"
    }
    icons["case"] = icons.select
    icons.loopbegin = {
    	iconback: "#3E3B3B"
    }
    icons.loopend = icons.loopbegin
    return {
        props : props,
        icons : icons
    }
}

function buildStrict() {
    var props = {}
    var icons = {}
    props.back=blue
    props.iconline = "#000000"
    props.iconback="#ffffff"
    props.iconback2="#ffffff"
    props.shadow="none"
    props.thickness=1
    props.style="solid"
    props.operator = "#cc3399"
    props.keyword = "#003399"
    props.number = "#004d00"
    props.string = "#800000"
    props.comment = "#995c00"
    props.error = "#ff0000"
    return {
        props : props,
        icons : icons
    }
}

function buildWhite1() {
    var props = {}
    var icons = {}
    props.back="#ffffff"
    props.iconline="#000000"
    props.iconback="#ffffff"
    props.iconback2="#ffffff"
    props.shadow="none"
    props.thickness=1
    props.style="solid"
    props.operator = "#000000"
    props.keyword = "#000000"
    props.number = "#000000"
    props.string = "#000000"
    props.comment = "#000000"
    props.identifier = "#000000"
    props.error = "#ff0000"
    return {
        props : props,
        icons : icons
    }
}

function buildWhite2() {
    var props = {}
    var icons = {}
    props.back="#f0f0f0"
    props.iconline="#c0c0c0"
    props.iconback="#ffffff"
    props.iconback2="#ffffff"
    props.shadow="50"
    props.thickness=1
    props.style="solid"
    props.operator = "#cc3399"
    props.keyword = "#003399"
    props.number = "#004d00"
    props.string = "#800000"
    props.comment = "#995c00"
    props.error = "#ff0000"
    return {
        props : props,
        icons : icons
    }
}

function get(prop) {
    return getProp(module, prop)
}

function getDefaultValue(prop) {
    var def
    def = module.def
    if (prop in def) {
        return def[prop]
    } else {
        return "#000000"
    }
}

function getIconProp(theme, type, prop) {
    var icon, result
    icon = theme.icons[type]
    if (icon === undefined) {
        return getProp(theme, prop)
    } else {
        result = icon[prop]
        if (result) {
            return result
        } else {
            return getProp(theme, prop)
        }
    }
}

function getProp(theme, prop) {
    if (prop in theme.props) {
        return theme.props[prop]
    } else {
        return getDefaultValue(prop)
    }
}

function getTheme(key) {
    var theme
    theme = module.themes[key]()
    return {
        props : Utils.copyObject(theme.props),
        icons : Utils.copyObject(theme.icons)
    }
}

function getThemes() {
    return Object.keys(module.themes)
}

function icon(type, prop) {
    return getIconProp(module, type, prop)
}

function makeThemeList() {
    module.themes = {
        "default" : buildDefaultTheme,
        white1 : buildWhite1,
        white2 : buildWhite2,
        dark1 : buildDark1,
        parrot : buildParrot,
        night : buildNight,
        cloud : buildCloud,
        strict : buildStrict,
        blue : buildBlue,
        redBlue : buildRedBlue
    }
}

function selectTheme(key) {
    var theme
    theme = module.themes[key]()
    setThemeValues(theme)
}

function setThemeValues(theme) {
    module.props = Utils.copyObject(theme.props)
    module.icons = Utils.copyObject(theme.icons)
}


this.get = get
this.getProp = getProp
this.icon = icon
this.getIconProp = getIconProp
this.selectTheme = selectTheme
this.getThemes = getThemes
this.getTheme = getTheme
this.setThemeValues = setThemeValues

module.def = buildDefaultTheme().props
makeThemeList()
selectTheme("white2")
}
var Theme = new ThemeUtils();
